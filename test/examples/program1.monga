/*
*********************** 
	Test Program 1 
***********************
 Just an ordinary program 
 to test data types and 
 ordinary data structures
*/
void main()
{
	char value;
	float fvalue;
	char[] array;
	if(value > 0)
	{
		/* only need to alloc array inside the if */
		array = new char[3];
		fvalue = 87 as float; 
		array[1] = value; /* need to overwrite the array */
	}
	else
	{
		fvalue = 90 as float;
	}
	@ "value is "; @fvalue; @ "\n";
	@ "END\n" ;
	return; /* no errors */
}